# Used when building for GitHub Pages (npm run build).
# The backend must be running on this machine at this address for the deployed
# frontend to work — GitHub Pages only hosts the static frontend, never the backend.
VITE_API_URL=http://localhost:8000/api/v1
